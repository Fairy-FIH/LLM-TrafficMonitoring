<UserControl x:Class="LlmUsageMonitor.App.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,14">
            <Button DockPanel.Dock="Right" Content="清空日志" Command="{Binding ClearCommand}" />
            <StackPanel>
                <TextBlock Text="运行日志" Style="{StaticResource PageTitle}" />
                <TextBlock Text="{Binding StatusText}" Style="{StaticResource Caption}" />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="8">
            <ListBox ItemsSource="{Binding Entries}" Background="Transparent" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel Margin="2,2">
                            <TextBlock DockPanel.Dock="Left" Text="{Binding TimeText}" Width="70"
                                       Foreground="{DynamicResource App.SubText}" FontFamily="Consolas" />
                            <TextBlock DockPanel.Dock="Left" Text="{Binding Level}" Width="60"
                                       Foreground="{Binding Level, Converter={StaticResource LogLevelToBrush}}"
                                       FontFamily="Consolas" />
                            <TextBlock Text="{Binding Message}" TextWrapping="Wrap"
                                       Foreground="{DynamicResource App.Text}" />
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</UserControl>
